package com.Object.collection;

public interface TestSet {
	/**
	 * 和List一样，可以看成是可变长度的数组的接口，有很多实现类
	 * 但是Set是一种不能包含重复元素的集合，即对于满足e1.eauqls(e2)条件的e1和e2
	 * 对象元素，不能同时存在于同一个set集合里
	 * 因此：set集合里的元素的实现类实现一个有效的eauqls(object)方法
	 * */
	
	/**
	 * Set的实现类中：推荐：HashSet、TreeSet
	 * */
}
